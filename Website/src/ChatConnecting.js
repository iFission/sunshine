import React, { Component } from 'react'

export default class ChatConnecting extends Component {
    render() {
        return (
            <React.Fragment>
                Connecting
            </React.Fragment>
        )
    }
}
