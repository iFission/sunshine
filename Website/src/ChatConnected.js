import React, { Component } from 'react'

export default class ChatConnected extends Component {
    render() {
        return (
            <React.Fragment>
                Connected
            </React.Fragment>
        )
    }
}
